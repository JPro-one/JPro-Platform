# Mirror JavaFX styleClass and id onto the rendered DOM as `jpro-<class>` / `jpro-<id>`.
# Required for Playwright to select nodes by id (e.g. setId("foo") -> "#jpro-foo").
jpro.mirrorCSSToDOM = true
